
import { 
  clientToDbIdMap, 
  dbToClientIdMap,
  mapClientToDbId,
  mapDbToClientId,
  expandClientToDbIds,
  expandClientToDbPackageIds
} from '@/utils/consultation/serviceIdMapper';

// Re-export all mapping functions
export {
  clientToDbIdMap,
  dbToClientIdMap,
  mapClientToDbId,
  mapDbToClientId,
  expandClientToDbIds,
  expandClientToDbPackageIds
};
